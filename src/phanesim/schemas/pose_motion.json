{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "pose_motion.json",
  "title": "PoseMotion",
  "description": "A motion description: which pose asset is reached at which time. Bone values are not stored here; they are resolved from the pose assets inside the model .blend at render time.",
  "type": "object",
  "required": [
    "name",
    "duration_ns",
    "events"
  ],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string"
    },
    "model": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "string"
        }
      ],
      "description": "Path to the .blend the pose assets were enumerated from, recorded for reference."
    },
    "seed": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "integer"
        }
      ],
      "description": "Seed of the random draw; re-running with this seed reproduces the timeline."
    },
    "duration_ns": {
      "type": "integer",
      "exclusiveMinimum": 0,
      "description": "Length of the timeline in nanoseconds."
    },
    "event_count": {
      "type": "integer",
      "minimum": 1,
      "description": "Derived: number of poses in the timeline, including the one at t=0."
    },
    "events": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/pose_event"
      }
    }
  },
  "$defs": {
    "pose_event": {
      "type": "object",
      "required": [
        "t_ns",
        "poses"
      ],
      "additionalProperties": false,
      "properties": {
        "t_ns": {
          "type": "integer",
          "minimum": 0,
          "description": "Time at which the asset is fully reached, in nanoseconds."
        },
        "group": {
          "type": "string",
          "description": "Body region the asset drives (left, right, head, body). Events in different groups touch disjoint bones and are layered rather than replacing one another."
        },
        "poses": {
          "type": "array",
          "minItems": 1,
          "description": "The assets this event sets, applied in order. One per joint part: arm, forearm, wrist and some fingers, since an asset drives only its own joint and one alone barely changes the picture.",
          "items": {
            "type": "object",
            "required": [
              "asset"
            ],
            "additionalProperties": false,
            "properties": {
              "asset": {
                "type": "string",
                "description": "Name of the pose asset (an action inside the model .blend)."
              },
              "alpha": {
                "type": "number",
                "minimum": 0.0,
                "maximum": 1.0,
                "description": "How far to blend it in: the bones it drives move this fraction of the way from wherever they are to the pose. 1.0 lands on the pose exactly."
              }
            }
          }
        }
      },
      "description": "One configuration the body reaches at one instant, built from several single-joint poses blended together."
    }
  }
}
